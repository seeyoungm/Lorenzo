__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Lorenzo Forge generated artifacts (reproducible via CLI, not source)
lorenzo_forge/data/
lorenzo_forge/artifacts/
# Released model weights are large binaries; model_card.json can be committed with -f
lorenzo_forge/releases/

# Scratch driver/log files from ad-hoc training runs
scratchpad*
